'use client'

/** `/comms/radio` — as frequências da família (COMMS-T12 / D-188). */
import CommsSections from '@/components/world-v2/CommsSections'

export default function Page() {
  return <CommsSections section="radio" />
}
