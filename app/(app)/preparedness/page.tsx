'use client'

/**
 * /preparedness — unified PREP-T01 surface.
 *
 * Resources and Checklist now live together because a family does not think in
 * two tabs: it needs to know what it has, what is missing, and what to do next.
 */

import PreparednessPage from '@/components/world-v2/PreparednessPage'

export default function Page() {
  return <PreparednessPage />
}
